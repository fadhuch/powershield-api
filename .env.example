# MongoDB Configuration
# Get this from your MongoDB Atlas dashboard -> Connect -> Connect your application
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority
# Example: mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/powershield?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/powershield?retryWrites=true&w=majority

# Database name (optional, will use the one in URI if not specified)
DB_NAME=powershield

# Server Configuration
NODE_ENV=development
PORT=5001

# JWT Configuration (for career management authentication)
JWT_SECRET=your_super_secret_jwt_key_here_minimum_32_characters_long

# MongoDB Atlas Troubleshooting Tips:
# 1. Replace 'username' and 'password' with your actual credentials
# 2. Replace 'cluster.mongodb.net' with your actual cluster URL
# 3. Ensure your IP address is whitelisted in MongoDB Atlas Network Access
# 4. Make sure your database user has readWrite permissions
# 5. Check that your cluster is not paused in Atlas

# Add any other environment variables your application needs
